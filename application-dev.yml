spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/senla_financial_manager
    username: postgres
    password: 1111
server:
  port: 8080
  # 8081